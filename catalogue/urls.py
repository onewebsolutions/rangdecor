from django.conf.urls import url
from . import views

urlpatterns = [

    # url(r'^catalogue_back/$', views.catalogue_back, name='catalogue_back'),
    # url(r'^catalogue_back/edit/(?P<cata_pk>\d+)/$', views.catalogue_back_edit, name='catalogue_back_edit'),

    # url(r'^catalogue_back/(?P<cata_pk>\d+)/page/$', views.catalogue_back_page, name='catalogue_back_page'),
    # url(r'^catalogue_back/(?P<cata_pk>\d+)/page/(?P<page_pk>\d+)/edit/$', views.catalogue_back_page_edit, name='catalogue_back_page_edit'),
    # url(r'^catalogue_back/(?P<cata_pk>\d+)/page/(?P<page_pk>\d+)/delete/$', views.catalogue_back_page_delete, name='catalogue_back_page_delete'),

]